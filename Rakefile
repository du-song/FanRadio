require "rubygems"
require "rake"

require "choctop"

ChocTop::Configuration.new do |s|
  # Remote upload target (set host if not same as Info.plist['SUFeedURL'])
  # s.host     = 'fanradio.com'
  s.remote_dir = "/Users/freewizard/Projects/AppEngine/rollingcode/static/app/fanradio"

  # Custom DMG
  # s.background_file = "background.jpg"
  # s.app_icon_position = [100, 90]
  # s.applications_icon_position =  [400, 90]
  s.volume_icon = "FanRadio.icns"
  # s.applications_icon = "appicon.icns" # or "appicon.png"
end
